driver=chrome
run.type=local
timeout.wait.element=10
timeout.wait.pageLoad=20
domain=${domain}
device=${sila.name}
username=${username}
password=${password}
mldpassword=${mld.password}
multithread=false